# Gradio UI
gradio>=6.14.0

# Charts
plotly>=5.18.0

# REST API
fastapi>=0.115.2
uvicorn[standard]>=0.29.0

# HuggingFace (LLM-as-Judge, future)
# Pinned <1.0 because the HF Spaces base image ships an older gradio
# (pre-5.7.1) that imports HfFolder from huggingface_hub, which was
# removed in huggingface_hub 1.0.0. The base-image gradio cannot be
# overridden via pip install — pinning the dependency is the documented
# fix on the HF Spaces forum for this exact crash.
huggingface_hub>=0.33.5,<1.0
requests>=2.31.0
