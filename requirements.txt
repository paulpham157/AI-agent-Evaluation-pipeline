# Gradio UI
gradio>=6.14.0

# Environment
python-dotenv>=1.0.0

# Charts
plotly>=5.18.0

# REST API
fastapi>=0.115.2
uvicorn[standard]>=0.29.0

# HuggingFace (LLM-as-Judge, future)
# Pinned to exact 0.x version (0.36.2 is the last 0.x release) because
# the HF Spaces base image ships an older gradio (pre-5.7.1) that does
# 'from huggingface_hub import HfFolder' in oauth.py. HfFolder was
# removed in huggingface_hub 1.0.0. A range pin (e.g. <1.0) is not
# enough: pip keeps the higher version already in the base image if the
# range still satisfies it. Exact pin forces a downgrade. The downgrade
# is also forced again in pre-requirements.txt for extra safety.
huggingface_hub==0.36.2
requests>=2.31.0
